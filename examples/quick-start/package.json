{
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "start-local": "LOCAL_ALIAS=true vite",
    "start-local-luma": "webpack-dev-server --env.local --env.local-luma --progress --hot --open",
    "clean": "rm -rf yarn.lock ./node_modules",
    "bootstrap": "yarn clean && yarn"
  },
  "peerDependencies": {
    "@luma.gl/debug": "^8.2.0"
  },
  "dependencies": {
    "d3-color": "^1.4.1",
    "@hubble.gl/core": "1.3.7",
    "@hubble.gl/react": "1.3.7",
    "popmotion": "^11.0.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^3.0.4"
  }
}
